name: skills
description: A CLI for managing your Dart skills.
version: 0.0.1
repository: https://github.com/serverpod/skills

executables:
  skills:

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.7.0
  config: ^0.8.0
  glob: ^2.1.0
  package_config: ^2.1.0
  path: ^1.9.0
  yaml: ^3.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  test_descriptor: ^2.0.0
